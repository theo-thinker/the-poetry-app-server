<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sakura.poetry.mapper.UserCollectionMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, poetry_id, folder_id, notes, created_time
    </sql>

    <!-- 根据用户ID和诗词ID查询收藏信息 -->
    <select id="selectByUserAndPoetry" resultType="com.sakura.poetry.entity.UserCollection">
        SELECT <include refid="Base_Column_List"/>
        FROM user_collection
        WHERE user_id = #{userId} AND poetry_id = #{poetryId}
    </select>

    <!-- 查询用户的收藏列表 -->
    <select id="selectByUserId" parameterType="long" resultType="com.sakura.poetry.entity.UserCollection">
        SELECT <include refid="Base_Column_List"/>
        FROM user_collection
        WHERE user_id = #{userId}
        ORDER BY created_time DESC
    </select>

    <!-- 查询用户在指定收藏夹中的收藏列表 -->
    <select id="selectByUserAndFolder" resultType="com.sakura.poetry.entity.UserCollection">
        SELECT <include refid="Base_Column_List"/>
        FROM user_collection
        WHERE user_id = #{userId} AND folder_id = #{folderId}
        ORDER BY created_time DESC
    </select>

    <!-- 删除用户的收藏记录 -->
    <delete id="deleteByUserAndPoetry">
        DELETE FROM user_collection
        WHERE user_id = #{userId} AND poetry_id = #{poetryId}
    </delete>

</mapper>