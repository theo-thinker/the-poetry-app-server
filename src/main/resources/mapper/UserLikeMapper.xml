<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sakura.poetry.mapper.UserLikeMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, target_type, target_id, created_time
    </sql>

    <!-- 根据用户ID、目标类型和目标ID查询点赞信息 -->
    <select id="selectByUserAndTarget" resultType="com.sakura.poetry.entity.UserLike">
        SELECT <include refid="Base_Column_List"/>
        FROM user_like
        WHERE user_id = #{userId} AND target_type = #{targetType} AND target_id = #{targetId}
    </select>

    <!-- 查询用户的所有点赞记录 -->
    <select id="selectByUserId" parameterType="long" resultType="com.sakura.poetry.entity.UserLike">
        SELECT <include refid="Base_Column_List"/>
        FROM user_like
        WHERE user_id = #{userId}
        ORDER BY created_time DESC
    </select>

    <!-- 查询指定目标的点赞数量 -->
    <select id="countByTarget" resultType="int">
        SELECT COUNT(*) FROM user_like
        WHERE target_type = #{targetType} AND target_id = #{targetId}
    </select>

    <!-- 删除用户的点赞记录 -->
    <delete id="deleteByUserAndTarget">
        DELETE FROM user_like
        WHERE user_id = #{userId} AND target_type = #{targetType} AND target_id = #{targetId}
    </delete>

</mapper>