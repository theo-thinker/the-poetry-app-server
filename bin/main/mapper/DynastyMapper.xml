<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sakura.poetry.mapper.DynastyMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, dynasty_name, dynasty_code, start_year, end_year, description, sort_order,
        status, created_by, created_time, updated_by, updated_time, is_deleted
    </sql>

    <!-- 根据编码查询朝代信息 -->
    <select id="selectByDynastyCode" parameterType="string" resultType="com.sakura.poetry.entity.Dynasty">
        SELECT <include refid="Base_Column_List"/>
        FROM dynasty
        WHERE dynasty_code = #{dynastyCode} AND is_deleted = 0
    </select>

    <!-- 查询启用的朝代列表 -->
    <select id="selectEnabledDynastyList" resultType="com.sakura.poetry.entity.Dynasty">
        SELECT <include refid="Base_Column_List"/>
        FROM dynasty
        WHERE status = 1 AND is_deleted = 0
        ORDER BY sort_order ASC, start_year ASC
    </select>

    <!-- 查询朝代列表（分页） -->
    <select id="selectDynastyList" parameterType="com.sakura.poetry.entity.Dynasty" resultType="com.sakura.poetry.entity.Dynasty">
        SELECT <include refid="Base_Column_List"/>
        FROM dynasty
        WHERE is_deleted = 0
        <if test="dynastyName != null and dynastyName != ''">
            AND dynasty_name LIKE CONCAT('%', #{dynastyName}, '%')
        </if>
        <if test="dynastyCode != null and dynastyCode != ''">
            AND dynasty_code LIKE CONCAT('%', #{dynastyCode}, '%')
        </if>
        <if test="status != null">
            AND status = #{status.value}
        </if>
        ORDER BY sort_order ASC, start_year ASC
    </select>

    <!-- 逻辑删除 -->
    <update id="logicDeleteById" parameterType="long">
        UPDATE dynasty
        SET is_deleted = 1
        WHERE id = #{id}
    </update>

</mapper>