<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sakura.poetry.mapper.CollectionFolderMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, folder_name, description, cover_image, poetry_count, is_public, sort_order,
        status, created_by, created_time, updated_by, updated_time, is_deleted
    </sql>

    <!-- 根据用户ID查询收藏夹列表 -->
    <select id="selectByUserId" parameterType="long" resultType="com.sakura.poetry.entity.CollectionFolder">
        SELECT <include refid="Base_Column_List"/>
        FROM collection_folder
        WHERE user_id = #{userId} AND is_deleted = 0
        ORDER BY sort_order ASC, created_time DESC
    </select>

    <!-- 查询用户的公开收藏夹列表 -->
    <select id="selectPublicByUserId" parameterType="long" resultType="com.sakura.poetry.entity.CollectionFolder">
        SELECT <include refid="Base_Column_List"/>
        FROM collection_folder
        WHERE user_id = #{userId} AND is_public = 1 AND is_deleted = 0
        ORDER BY sort_order ASC, created_time DESC
    </select>

    <!-- 增加收藏夹中的诗词数量 -->
    <update id="incrementPoetryCount">
        UPDATE collection_folder
        SET poetry_count = poetry_count + #{count}
        WHERE id = #{folderId}
    </update>

    <!-- 逻辑删除 -->
    <update id="logicDeleteById" parameterType="long">
        UPDATE collection_folder
        SET is_deleted = 1
        WHERE id = #{id}
    </update>

</mapper>