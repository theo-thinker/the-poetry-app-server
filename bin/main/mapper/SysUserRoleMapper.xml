<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sakura.poetry.mapper.SysUserRoleMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, role_id, created_by, created_time
    </sql>

    <!-- 根据用户ID查询用户角色关联列表 -->
    <select id="selectByUserId" parameterType="long" resultType="com.sakura.poetry.entity.SysUserRole">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_user_role
        WHERE user_id = #{userId}
        ORDER BY created_time DESC
    </select>

    <!-- 根据角色ID查询用户角色关联列表 -->
    <select id="selectByRoleId" parameterType="long" resultType="com.sakura.poetry.entity.SysUserRole">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_user_role
        WHERE role_id = #{roleId}
        ORDER BY created_time DESC
    </select>

    <!-- 删除用户的所有角色关联 -->
    <delete id="deleteByUserId" parameterType="long">
        DELETE FROM sys_user_role
        WHERE user_id = #{userId}
    </delete>

    <!-- 删除角色的所有用户关联 -->
    <delete id="deleteByRoleId" parameterType="long">
        DELETE FROM sys_user_role
        WHERE role_id = #{roleId}
    </delete>

</mapper>