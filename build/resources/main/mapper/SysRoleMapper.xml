<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sakura.poetry.mapper.SysRoleMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, role_name, role_code, description, sort_order, status, created_by, created_time, updated_by, updated_time, is_deleted
    </sql>

    <!-- 根据角色编码查询角色信息 -->
    <select id="selectByRoleCode" parameterType="string" resultType="com.sakura.poetry.entity.SysRole">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_role
        WHERE role_code = #{roleCode} AND is_deleted = 0
    </select>

    <!-- 查询角色列表（分页） -->
    <select id="selectRoleList" parameterType="com.sakura.poetry.entity.SysRole" resultType="com.sakura.poetry.entity.SysRole">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_role
        WHERE is_deleted = 0
        <if test="roleName != null and roleName != ''">
            AND role_name LIKE CONCAT('%', #{roleName}, '%')
        </if>
        <if test="roleCode != null and roleCode != ''">
            AND role_code LIKE CONCAT('%', #{roleCode}, '%')
        </if>
        <if test="status != null">
            AND status = #{status.value}
        </if>
        ORDER BY sort_order ASC, created_time DESC
    </select>

    <!-- 查询用户拥有的角色列表 -->
    <select id="selectRolesByUserId" parameterType="long" resultType="com.sakura.poetry.entity.SysRole">
        SELECT r.<include refid="Base_Column_List"/>
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.is_deleted = 0 AND r.status = 1
        ORDER BY r.sort_order ASC
    </select>

    <!-- 逻辑删除 -->
    <update id="logicDeleteById" parameterType="long">
        UPDATE sys_role
        SET is_deleted = 1
        WHERE id = #{id}
    </update>

</mapper>